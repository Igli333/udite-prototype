apiVersion: v1
kind: Service
metadata:
  name: ms-simulator
  namespace: simulator
spec:
  selector:
    app: ms-simulator
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
  type: LoadBalancer