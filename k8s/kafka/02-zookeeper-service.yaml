apiVersion: v1
kind: Service
metadata:
  labels:
    app: zookeeper-service
  name: zookeeper-service
  namespace: kafka
spec:
  type: NodePort
  ports:
    - port: 2181
      name: zookeeper-port
      nodePort: 30181
      targetPort: 2181
  selector:
    app: zookeeper