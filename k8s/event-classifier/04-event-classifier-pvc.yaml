apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: event-classifier-claim
  namespace: event-classifier
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
